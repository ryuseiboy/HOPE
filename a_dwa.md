# Role
あなたは熟練したロボット工学エンジニアであり、Pythonのエキスパートです。
以下の学術論文の記述に基づき、移動ロボットの経路計画アルゴリズムである「Kinematically Constrained A* Algorithm and DWA Fusion Algorithm」をPythonで実装してください。

# Key Technical Specifications (From Paper)

## 1. Global Planner: Improved A* Algorithm
従来のA*ではなく、論文のSection 3で提案された以下の改良を実装すること。
- **ヒューリスティック関数:** マンハッタン距離を使用。
  - $D = |x_i - x_j| + |y_i - y_j|$
- **評価関数 (Dynamic Weighting):**
  - $f(n) = g(n) + e^P \cdot h(n)$
  - ここで $P$ は距離スケールパラメータ: $P = \frac{|x_n - x_g| + |y_n - y_g|}{|x_s - x_g| + |y_s - y_g|}$ ($s$: start, $g$: goal, $n$: current)
- **近傍探索の最適化 (Neighborhood Optimization):**
  - 親ノードからターゲットへの角度に基づき、探索すべき子ノードの方向を制限する（冗長な方向を探索しない）。
  - ※実装が複雑な場合、まずは通常の8近傍探索で実装し、TODOコメントとして残してもよい。

## 2. Local Planner: Optimized DWA (Dynamic Window Approach)
- **評価関数:**
  - $G(v,w) = \alpha \cdot vel(v,w) + \beta \cdot head(v,w) + \gamma \cdot dist(v,w)$
- **重み係数 (Optimized Weights):**
  - $\alpha = 15$
  - $\beta = 37$
  - $\gamma = 0.02$
- **処理:**
  - ロボットのキネマティクス（速度・加速度制限）を考慮したDynamic Windowを作成。
  - 評価関数が最大となる $(v, w)$ を選択して制御指令とする。

## 3. 「改良A*アルゴリズム（グローバル計画）」と「DWA（ローカル計画）」の融合（Fusion）手法フローチャート

### 1. 左側のフロー：グローバル経路計画（A*アルゴリズム）
ここでは、障害物を避けた「スタートからゴールまでの最適なルート」を地図上で計算します。

1.  **初期化 (Start 〜 Add the initial node...)**
    * スタート地点を「オープンリスト（探索候補のリスト）」に入れます。
2.  **最良ノードの選択 (Iterate through...)**
    * オープンリストの中から、評価関数 $f(n)$（移動コスト＋ゴールまでの推定距離）が最小のノードを選び、「カレントノード（現在着目する点）」とします。
3.  **探索済みへの登録 (Add ... to the closelist)**
    * そのノードを「クローズドリスト（探索済みリスト）」に移します。
4.  **周辺ノードの探索 (Search for reachable nodes...)**
    * カレントノードの周囲にある移動可能なノード（障害物以外）を探します。
5.  **経路の更新判断 (菱形の分岐群)**
    * **Is this node in the openlist? (すでに候補にあるか？)**
        * **No:** 新たに候補としてオープンリストに追加し、親ノード（どこから来たか）を記録します。
        * **Yes:** すでに候補にある場合、**「今回のルートで行く方が近道か？（$g(n)$が小さいか？）」**を判定します。近道なら親ノード情報を更新します。
6.  **ゴール判定 (Is the target node...? / Is openlist empty?)**
    * ターゲット（ゴール）が見つかれば、経路を保存して**右側のフローへ移行**します（End of path search, save path）。
    * 候補がなくなってもゴールに着けなければ「経路なし」として終了します。

---

### 2. 真ん中の接続：情報の受け渡し（Fusionの肝）

左側のA\*で作った経路をそのままDWAに渡すのではなく、ここで重要な処理が入っています。

* **Extraction of key nodes on the path（経路上のキーノード抽出）**
    * A\*で作られた経路は細かいグリッドの連続ですが、ロボットがスムーズに走るには細かすぎます。
    * そこで、**曲がり角や一定間隔ごとの重要な地点（キーノード）**だけを抜き出します。これらがDWAにとっての「当面の目印（サブゴール）」になります。

---

### 3. 右側のフロー：ローカル経路計画・走行（DWA）
ここでは、A\*が決めた「キーノード」に向かって、障害物を避けながら実際にロボットを動かします。

1.  **サブゴールの設定 (Use the key nodes as the target...)**
    * 抽出されたキーノードを、DWAが目指すべき一時的なターゲットに設定します。
2.  **速度サンプリング (Mobile robot speed (v,w) sampling)**
    * ロボットが出せる様々な速度（$v$）と回転速度（$w$）の組み合わせをシミュレーションします（「少し右に曲がりながら進む」「直進する」など）。
3.  **最適速度の決定 (Select the sampling speed...)**
    * 第4章で翻訳した**評価関数**（ゴール方向への向き、速度、障害物との距離）を使ってスコアを計算し、最もスコアが高い速度指令を採用してロボットを動かします。
4.  **到達判定のループ (菱形の分岐群)**
    * **Whether to reach the target node? (キーノードに着いたか？)**
        * **No:** 着いていなければ、再度速度サンプリングに戻り、走り続けます（この間に動く障害物があればDWAが避けます）。
        * **Yes:** キーノードに到達したら次へ進みます。
    * **Whether the target node is the end point? (そこは最終ゴールか？)**
        * **No:** まだ最終ゴールでなければ、**次のキーノード**を新たなターゲットに設定してループします。
        * **Yes:** 最終ゴールなら、運転終了（End）です。

---

### まとめ

このフローチャートが示しているのは、以下のような役割分担です。

* **頭脳（左側 A*）:** 地図全体を見て、迷路の正解ルートを見つける。「あっちを通って、あの角を曲がればいい」と計画する。
* **反射神経（右側 DWA）:** 目の前の状況を見てハンドルを切る。「計画通りに進むけど、今ここに人が飛び出してきたから避ける」「次の目印までスムーズに加速する」といった動作を行う。

**「A\*の結果からキーノードを抽出し、それをDWAの目標点として順次与えていく」**という構造が、この論文の提案する「融合アルゴリズム」の正体です。
